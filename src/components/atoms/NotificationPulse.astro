---
const { id } = Astro.props;
---

<span class="relative flex w-2 h-2" id={id}>
  <span
    class="absolute inline-flex w-full h-full rounded-full opacity-75 animate-ping bg-rose-500"
  ></span>
  <span class="relative inline-flex w-2 h-2 rounded-full bg-rose-700"></span>
</span>
